name: controller
version: v1.0

discovery:
  endpoints: tcp://127.0.0.1:26688
  user: root
  password: 123456

service:
  listenAddress: tcp://127.0.0.1:26689

log:
  fileName: log/controller.log
  fileSize: 100
  maxBackupCount: 10
  maxBackupAge: 10
